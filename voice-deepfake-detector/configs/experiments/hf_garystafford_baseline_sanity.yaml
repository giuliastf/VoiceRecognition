extends: ../base.yaml

experiment:
  name: exp011_hf_garystafford_baseline_sanity
  split: baseline

cache:
  enabled: false

hf_model:
  model_name: garystafford/wav2vec2-deepfake-voice-detector
  fake_labels: ["fake", "spoof", "spoofed"]
  real_labels: ["real", "bonafide", "genuine"]
  fake_label_id: 1
  max_test_samples: 500
