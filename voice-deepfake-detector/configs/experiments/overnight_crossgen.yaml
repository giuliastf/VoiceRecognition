extends: ../base.yaml

experiment:
  name: exp004_overnight_crossgen
  split: crossgen

cache:
  enabled: true

augment:
  enabled: true

training:
  batch_size: 32
  epochs: 12
  learning_rate: 0.0002
  weight_decay: 0.0001
  max_train_samples_per_epoch: 60000
  max_val_samples: 12000
  early_stopping_patience: 3
